// const init = require("./config/socket")

const io = require("./config/socket")
const { SOCKET_PORT } = require("./const")

const run = () => {
    try {
    } catch (e) {
        console.log('error')
    }
}

run()